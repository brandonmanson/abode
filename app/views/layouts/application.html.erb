<!DOCTYPE html>
<html ng-app="abode">
<head>
  <title>Abode</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
  <nav>
    <ul>
      <% if signed_in? %>
        <li><%= gravatar_for current_user %></li>
        <li><%= link_to "Profile", user_show_path(current_user) %></li>
        <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
        <% if current_user.dwelling != nil %>
          <li><%= link_to "Abode", dwelling_show_path(current_user.dwelling) %></li>
        <% end %>
      <% else %>
        <li><%= link_to "Sign in", signin_path %></li>
        <li><%= link_to "Register", signup_path %></li>
      <% end %>
    </ul>
  </nav>
  <div class="container">
    <%= yield %>
  </div>
  <footer>
    <p>&copy; 2014 Flock of Squirrels Studios</p>
  </footer>
</body>
</html>
