<%= flash[:message] %>
<%= form_for @invitation, url: {controller: 'invitations', action: 'create' } do |f| %>
  <%= f.text_field :email, placeholder: "Email" %>
  <%= f.submit "Invite Roommates" %>
<% end  %>
