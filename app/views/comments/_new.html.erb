<%= flash[:error] %>
<%= form_for [dwelling, comment], :url => abode_comments_path(dwelling, comment) do |f| %>
  <%= f.text_area :description, size: "40x20" %>
  <%= f.submit "Add Comment" %>
<% end %>
